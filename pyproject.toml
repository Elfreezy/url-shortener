[project]
name = "code"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.128.7",
    "loguru>=0.7.3",
    "pydantic-settings>=2.12.0",
    "pyshorteners>=1.0.1",
    "pytest>=9.0.2",
    "uvicorn>=0.40.0",
]
